#include "main.h"
/**
 * _islower - program that checks for lowercase character
 *
 * Return 1 if lowercase, otherwise return 0
 */
int _islower(int c)
{
	int i = islower(c);

	if (i < z)
	{
		return (1);
	}
	else 
	{
		return (0)
	}
}
