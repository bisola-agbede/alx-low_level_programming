#include "main.h"
/**
 * _isupper - checks for an uppercase character
 *
 * @c: variable to check if uppercase
 *
 * Return: 1 if uppercase, 0 otherwise
 */
int _isupper(int c)
{
	if (c >= 'A' && C <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
